version: '3.7'

services:

  react:
    container_name: react
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 4000:3006
    environment:
      - CHOKIDAR_USEPOLLING=true2